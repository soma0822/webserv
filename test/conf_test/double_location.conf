server {
  server_name localhost;
  server_name localhost2;
  listen 8080;
  host localhost;
  error_page 404 error_index.html;
  location / {
    root /;
  }
  location / {
    error_page 400 error_index.html;
  }
}
